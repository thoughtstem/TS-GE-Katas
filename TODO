
require with prefix
  > that's fine
start aliasing
  > make an (alias ...) form
  > need syntax for altering keyword defaults.  Note that this will need to propagate into the srcdoc submodule, call define/contract/provide?
don't forget to provide
  > alias will provide (and do srcsdoc).  will also keep track of aliases, so examples can use...
write example
  > nope, will declare (like lang->examples ...)  Use that?
  > (import-examples battle-arena #:with-aliases "../lang/main.rkt")

In a skinned language, do we do define/contract/doc (or auto docs at all?)
  Currently breaks [Errors on build with default scribble files...] if we don't...

Skinned langs:
  Begin hand-crafting a skinned lang
  Create tools
  Write code
  Doc things
  Make the second time easier. 

Instead of learning new things: Catch up to yourself.  Faster.

WEDNESDAY
  - Stephen pivots to online course stuff 
  - Orr..... Stephen has to work on backend bullshit for a while.  We'll see...

BY THURSDAY AND During ireland:
  Start breaking down this project with the team.  Structure of kata collections?  Design process?  Todos?  Meta katas for "copyright law"...
  ...

  Extended languages...  Language skins...
  Starwars and the rest...
