#lang scribble/manual

@title{Game Show}

Use these questions during Game Show time. You can ask any/all the questions in each day's section.

@section{Monday}

@itemlist[@item{@bold{What keys make your character move?}
                    @itemlist[@item{@italic{Arrow keys.}}]}
          @item{@bold{What is the keyword to add a character to your game?}
                    @itemlist[@item{@italic{#:wizard}}]}
          @item{@bold{What is the keyword to change what your character looks like?}
                    @itemlist[@item{@italic{#:sprite}}]}
          @item{@bold{Name 3 available character sprites.}
                    @itemlist[@item{@italic{Anything from the Assets Library.}}]}
          @item{@bold{What is the default speed value?}
                    @itemlist[@item{@italic{10.}}]}]


@section{Tuesday}

@itemlist[@item{@bold{How much health does a default potion heal}
                    @itemlist[@item{@italic{10.}}]}
          @item{@bold{What is the keyword to change how much health a potion heals?}
                    @itemlist[@item{@italic{#:heals-by}}]}
          @item{@bold{What is the keyword to change the number of potions in the game?}
                    @itemlist[@item{@italic{#:amount-in-world}}]}
          @item{@bold{What two things does the player need to make a potion?}
                    @itemlist[@item{@italic{Recipes and ingredients.}}]}
          @item{@bold{What does HD stand for when referring to images?}
                    @itemlist[@item{@italic{High Definition.}}]}]

@section{Wednesday}

@itemlist[@item{@bold{What is the keyword to make a quest?}
                    @itemlist[@item{@italic{#:quest-list}}]}
          @item{@bold{Describe what "loot" is.}
                    @itemlist[@item{@italic{Anything dropped by a deatheater.}}]}
          @item{@bold{Name a keyboard shortcut; a combination of keys.}
                    @itemlist[@item{@italic{CTRL + C, CTRL + V, CTRL + X, CTRL + A, CTRL + Z, CTRL + Y.}}]}
          @item{@bold{What are the keywords to trigger a cutscene?}
                    @itemlist[@item{@italic{spawn page}}]}
          @item{@bold{What does USB stand for?}
                    @itemlist[@item{@italic{Universal Serial Bus.}}]}]

@section{Thursday}

@itemlist[@item{@bold{How can you give an NPC random responses?}
                    @itemlist[@item{@italic{random-npc-response}}]}
          @item{@bold{Why is defining things outside of the game function considered good code?}
                    @itemlist[@item{@italic{It makes your code easier for someone else to understand.}}]}
          @item{@bold{How can you give NPCs dialog?}
                    @itemlist[@item{@italic{#:dialog (list ... ... ...)}}]}
          @item{@bold{Name two things that an NPC can do in a game.}
                    @itemlist[@item{@italic{Start a quest, have dialog, follow the player, be stored in the player backpack.}}]}
          @item{@bold{How can you change an NPC's dialog after their quest is complete?}
                    @itemlist[@item{@italic{#:new-response-dialog(list ... ... ...)}}]}]
